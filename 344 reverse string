class Solution {
    public void reverseString(char[] s) {
        
        int end = s.length-1;
        for(int start = 0; start<=end; start++){
            char temp = s[end];
            s[end] = s[start];
            s[start] = temp;
            // start++;
            end--;
        }
    }
}
